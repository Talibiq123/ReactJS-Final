import React from "react";

// Create the context: create an object which is global and can be accessed from anywhere in the App.

const CricketContext = React.createContext(); 

// console.log(CricketContext); // Global Object: value, PRovider and Consumer

export default CricketContext;